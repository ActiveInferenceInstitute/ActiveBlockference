# Runtime requirements for ActiveBlockference.
# For a uv-managed install with dev/docs extras, prefer:
#   uv venv && uv pip install -e ".[dev,docs]"
# A fully-pinned snapshot of the verified environment is kept in
# requirements.lock (generated via `uv pip freeze`).
inferactively-pymdp>=1.0.1
cadCAD>=0.4.28
radcad>=0.12
numpy>=1.24,<2.3
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
plotly>=5.0
networkx>=3.0
scikit-learn>=1.3
pyyaml>=6.0
