# Tests

The suite covers strict numerical primitives, closed configuration, canonical
grid dynamics, real radCAD/cadCAD runs, persisted artefacts, rendering,
notebooks, and the credential-free research workflow. Run it with
`uv run pytest` after `uv sync --locked --extra dev`.
