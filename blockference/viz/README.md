# Visualisation

Renderers validate trajectory structure, grid dimensions, agent identifiers,
FPS, and output extensions. `plot_efe` plots persisted expected-free-energy
vectors; no inferred proxy is substituted. Every renderer closes its figure on
success and failure.
