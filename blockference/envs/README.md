# Environments

`GridWorld` is the package environment. It uses `(y, x)` coordinates, validates
all positions and actions, and resolves simultaneous transitions
deterministically: occupied targets, competing targets, and swaps are rejected.
