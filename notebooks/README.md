# Notebooks

Every notebook is a small executable example of the current package API. The
examples cover the model, environment transitions, collision resolution, and
the optional local research workflow.
