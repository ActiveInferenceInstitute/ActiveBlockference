# Notebook guidance

Every notebook starts with a two-to-four sentence teaching summary, imports the
current `blockference` API, contains no absolute paths, and executes in a clean
temporary workspace. Keep outputs small and do not commit checkpoints. Shared
logic belongs in the package and is tested there.
